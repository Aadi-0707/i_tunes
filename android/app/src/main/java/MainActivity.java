package com.example.itunes; // <-- Make sure this matches your applicationId in build.gradle

import io.flutter.embedding.android.FlutterActivity;

public class MainActivity extends FlutterActivity {
    // No changes needed unless you plan to implement custom platform channels.
}
